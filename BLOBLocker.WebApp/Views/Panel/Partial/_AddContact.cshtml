@model BLOBLocker.Code.ViewModels.WebApp.AddContactViewModel

@using (Html.BeginForm("AddContact", "Panel", FormMethod.Post))
{
    Html.ValidationSummary();

    <text>@Html.LabelFor(p => p.AddAlias) @Html.TextBoxFor(p => p.AddAlias)</text>
    @Html.AntiForgeryToken()
    <input type="submit" value="@HttpContext.GetGlobalResourceObject(null, "Button.Add")" />
}